# Auto detect text files and perform LF normalization
* text=auto

# Preserve the official upstream OFL text byte-for-byte, including its trailing space.
assets/fonts/chenyuluoyan/license.txt -text whitespace=-trailing-space
